/* eslint-disable import/prefer-default-export */
/* eslint-disable import/no-unresolved */
import { createContext } from 'react';

// 利用 createContext 新增 AuthContext
export const AuthContext = createContext(null);
