<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit price</title>
  <%- include('template/header')%>
</head>

<body>
  <div class="container mt-3">
    <form action="/edit/<%= price.id %>" method="POST" class="mt-3" />
    <h3>編輯獎品</h3>
    <div class="mt-3">
      獎品序號：<input type=" number" name="number" value="<%= price.number %>" />
    </div>
    <div class="mt-3">
      獎品內容：<input type="text" name="content" value="<%= price.content %>" />
    </div>
    <div class="mt-3">
      中獎機率：<input type="number" name="probability" value="<%= price.probability %>%" />
    </div>
    <div class="mt-3">
      圖片網址：<input type="text" name="imageUrl" value="<%= price.imageUrl %>" />
    </div>
    <p class="errorMsg"><%= errorMsg %></p>
    <button type="submit" class="btn btn-dark btn-sm mt-3 price_editButton">提交</button>
    </form>
  </div>
</body>

</html>